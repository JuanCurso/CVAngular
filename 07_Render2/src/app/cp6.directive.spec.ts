import { Cp6Directive } from './cp6.directive';

describe('Cp6Directive', () => {
  it('should create an instance', () => {
    const directive = new Cp6Directive();
    expect(directive).toBeTruthy();
  });
});
