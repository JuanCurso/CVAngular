import { Cp7Directive } from './cp7.directive';

describe('Cp7Directive', () => {
  it('should create an instance', () => {
    const directive = new Cp7Directive();
    expect(directive).toBeTruthy();
  });
});
