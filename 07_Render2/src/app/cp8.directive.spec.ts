import { Cp8Directive } from './cp8.directive';

describe('Cp8Directive', () => {
  it('should create an instance', () => {
    const directive = new Cp8Directive();
    expect(directive).toBeTruthy();
  });
});
